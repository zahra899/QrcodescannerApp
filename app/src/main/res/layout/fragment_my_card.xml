<?xml version="1.0" encoding="utf-8"?>
<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D0D0D"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <!-- Top bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="20dp">

            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_arrow_back"
                app:tint="#FFFFFF" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="My Card"
                android:textColor="#FFFFFF"
                android:textSize="20sp"
                android:textStyle="bold" />

            <!-- balance the back button width -->
            <View
                android:layout_width="40dp"
                android:layout_height="40dp" />
        </LinearLayout>

        <!-- Repeated field: use include for each -->
        <include layout="@layout/item_card_field" android:id="@+id/fieldName" />
        <include layout="@layout/item_card_field" android:id="@+id/fieldPhone" />
        <include layout="@layout/item_card_field" android:id="@+id/fieldEmail" />
        <include layout="@layout/item_card_field" android:id="@+id/fieldAddress" />
        <include layout="@layout/item_card_field" android:id="@+id/fieldBirthday" />
        <include layout="@layout/item_card_field" android:id="@+id/fieldOrg" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enter Note"
            android:textColor="#00E6A8"
            android:textSize="12sp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="6dp" />

        <EditText
            android:id="@+id/etNote"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:background="@drawable/bg_field_dark"
            android:padding="14dp"
            android:gravity="top"
            android:hint="Add note"
            android:textColorHint="#666666"
            android:textColor="#FFFFFF"
            android:inputType="textMultiLine" />

        <ImageView
            android:id="@+id/imgQrPreview"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_gravity="center"
            android:layout_marginTop="24dp"
            android:background="#FFFFFF"
            android:padding="8dp" />

        <Button
            android:id="@+id/btnSaveCard"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:layout_marginTop="16dp"
            android:backgroundTint="#00E6A8"
            android:text="Save &amp; Generate QR"
            android:textColor="#0D0D0D"
            android:textStyle="bold" />

    </LinearLayout>
</androidx.core.widget.NestedScrollView>